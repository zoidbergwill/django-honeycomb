============
Installation
============

At the command line::

    $ easy_install django-honeycomb

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-honeycomb
    $ pip install django-honeycomb
